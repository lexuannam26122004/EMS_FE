// pages/settings/security.tsx
import Layout from '../../components/Layout'

const Security = () => {
    return (
        <Layout>
            <h1>Security Settings</h1>
            <p>This is the content for security settings.</p>
        </Layout>
    )
}

export default Security
